type Payload=Record<string,string|boolean>;
const url=process.env.NEXT_PUBLIC_SUPABASE_URL?.replace(/\/$/,"")??"";
const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY??"";
const headers={apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json"};
function configured(){if(!url||!key)throw new Error("A conexão com o Supabase ainda não foi configurada.")}
export async function listPublishedBooks(){configured();const r=await fetch(`${url}/rest/v1/books?published=eq.true&select=id,title,author,category,description,publication_year,isbn,cover_url,purchase_url&order=publication_year.desc,id.desc`,{headers,cache:"no-store"});if(!r.ok)throw new Error("Catálogo temporariamente indisponível.");return(await r.json()).map((b:Record<string,unknown>)=>({id:b.id,title:b.title,author:b.author,category:b.category,description:b.description,publicationYear:b.publication_year,isbn:b.isbn,coverUrl:b.cover_url,purchaseUrl:b.purchase_url}))}
export async function createSubmission(data:Payload){configured();const protocol=`SIN-${new Date().getFullYear()}-${crypto.randomUUID().slice(0,8).toUpperCase()}`;const clean=(k:string)=>String(data[k]??"").trim().slice(0,6000);const body={protocol,author_name:clean("authorName"),email:clean("email"),phone:clean("phone"),biography:clean("biography"),title:clean("title"),subtitle:clean("subtitle"),category:clean("category"),synopsis:clean("synopsis"),audience:clean("audience"),differentiator:clean("differentiator"),stage:clean("stage"),word_count:clean("wordCount"),rights_confirmed:data.rightsConfirmed===true,cover_available:data.coverAvailable===true,reviewer_one:clean("reviewerOne"),reviewer_two:clean("reviewerTwo"),notes:clean("notes")};const r=await fetch(`${url}/rest/v1/submissions`,{method:"POST",headers:{...headers,Prefer:"return=minimal"},body:JSON.stringify(body)});if(!r.ok)throw new Error("Não foi possível registrar a proposta agora. Revise os campos e tente novamente.");return protocol}
